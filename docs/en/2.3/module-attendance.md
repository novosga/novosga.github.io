# Attendance module

https://github.com/novosga/attendance-bundle

As of version 2.3, attendance behavior settings (priority ticket interleaving and queue jumping) can be overridden individually per user, allowing each attendant to have settings that take precedence over the global system configuration.
